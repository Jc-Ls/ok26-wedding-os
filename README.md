# ok6wedding

Note: This project requires Node >=20.9.0 for Next.js 16 and Prisma. Use a Node version manager (nvm or volta) and run:

```bash
nvm install 20
nvm use 20
```

Also ensure a `DATABASE_URL` is provided in your environment before running `npm run build` (Prisma `prisma db push` requires it).